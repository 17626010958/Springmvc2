<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http:mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lpp.springmvc1.mapper.AreaMapper">
	<select id="findAll" resultType="com.lpp.springmvc1.pojo.Area">
		select * from bas_area
	</select>
</mapper>